# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build output of vite-plugin-pwa in dev mode
dev-dist

# Environment: every variant is ignored except the committed template.
.env
.env.*
!.env.example

# Test output
coverage
playwright-report
playwright/.cache
test-results

# Supabase CLI local state. `migrations/`, `tests/`, `seed.sql`, and `config.toml`
# are all committed — these are the per-machine scratch directories the CLI writes
# when a local stack is running.
supabase/.temp/
supabase/.branches/

# Vercel CLI local state (`vercel dev`, `vercel link`).
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
